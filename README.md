## Sample CRUD with Node.js, MongoDB, Express, and Dust

- install mongodb
- run mongodb in one terminal
- once db started, run mongo in another terminal
- create a new db by running 'use storedata'
- add new entry through the app.
